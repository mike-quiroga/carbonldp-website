repositories.apps.directory = /opt/carbon
repositories.platform.directory = /opt/carbon/platform/repository

platform.url = http://local.carbonldp.com/
platform.container = platform/
platform.container.url = ${platform.url}${platform.container}
platform.apps.container = apps/
platform.apps.container.url = ${platform.container.url}${platform.apps.container}
platform.agents.container = agents/
platform.agents.container.url = ${platform.container.url}${platform.agents.container}
platform.roles.container = roles/
platform.roles.container.url = ${platform.container.url}${platform.roles.container}
platform.privileges.container = privileges/
platform.privileges.container.url = ${platform.container.url}${platform.privileges.container}

apps.entrypoint = apps/
apps.entrypoint.url = ${platform.url}${apps.entrypoint}

generic-request = requests/
generic-request.url = ${platform.url}${generic-request}

authentication.realm-name = Carbon LDP

config.enforce-ending-slash = true